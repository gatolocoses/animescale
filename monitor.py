#!/usr/bin/env python3
"""Thin shim — delegates to animescale.monitor. Run directly or via `animescale-monitor` after pip install."""
from animescale.monitor import main

if __name__ == "__main__":
    main()
